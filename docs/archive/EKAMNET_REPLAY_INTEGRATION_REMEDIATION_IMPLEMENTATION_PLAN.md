# EKAMNET REPLAY INTEGRITY REMEDIATION IMPLEMENTATION PLAN
## DP / EKAMNET RESEARCH PROGRAM

This document outlines the implementation plan for repairing the three known Replay integrity defects in the active codebase.

---

### 1. Executive Summary

This plan provides a step-by-step remediation strategy for the three database and taxonomy integrity defects identified in the native backtest loop:
1. **Relational lineage ID propagation bug**: Lineage engine outputs are not written back to the Pydantic model before DB persistence.
2. **TheoryStructured ID/timestamp collisions**: Nested sub-objects are duplicated on deepcopy mutations.
3. **Ontology registry mismatch**: `SECTOR_ZSCORE` is omitted from `CORE_CONCEPTS` but generated by the LLM, leading to compliance failures.

The repairs are targeted, non-disruptive to cognitive logic, and lay the groundwork for verifying longitudinal lifecycle closure via Candidate F.

**Final Recommendation**: `REMEDIATION_PLAN_READY_FOR_CLAUDE_ADVERSARIAL_REVIEW`

---

### 2. Scope and Prohibitions

This is a **planning-only document**. In accordance with program steering constraints:
- No source code was modified.
- No database migrations or schema updates were executed.
- No backtest simulations were run.
- No cognitive rules or prompts were altered.
- No milestone closures or scientific validation claims were made.

---

### 3. Evidence Method

Code execution paths in `replay_engine.py`, `theory_lineage.py`, and `ontology.py` were traced to find the exact instructions where identifiers are assigned, copied, or validated. Diagnostic metrics from the 10-day backtest were reconciled to confirm root causes.

---

### 4. Governing Remediation Invariant

`REPAIR OBSERVABILITY AND TRACEABILITY WITHOUT CHANGING COGNITION`

The remediation must NOT alter:
- LLM prompt structures, temperatures, or seeds,
- Novelty gate decisions (`REINFORCE`/`REVISE`/`GENERATE`),
- Contradiction detection metrics or retirement thresholds,
- Prediction or trading policies.

---

### 5. Defect 1 — Relational Lineage Propagation Root Cause

* **Symptom**: Relational SQL tables save a static lineage ID (`7c039bc0-3c76-46d2-bd4b-5692252341e2`) for all theories, while `theory_lineage.json` tracks the correct parent-child lineage.
* **Root Cause Trace**: 
  1. On day 0, a new theory is compiled by `TheoryGenerationFlow.process` (line 557) and initialized with `lineage_id=str(uuid4())`. In one run, this random UUID evaluates to `7c039bc0-3c76-46d2-bd4b-5692252341e2`.
  2. On subsequent days, the novelty gate selects `REVISE` or `REINFORCE` and duplicates the day 0 object using `theory = copy.deepcopy(prior_theory)` (`replay_engine.py:1520` and `1838`).
  3. While `theory.id` is regenerated (`theory.id = str(uuid4())`), the field `theory.lineage_id` is left untouched, inheriting the static ID (`7c039bc0-3c76-46d2-bd4b-5692252341e2`) of the parent.
  4. In `replay_engine.py` line 2018, the lineage engine computes `lineage_id_val` representing the true evolving parent-child lineage node tree.
  5. The divergence point occurs because `lineage_id_val` is returned but never assigned back to `theory.lineage_id`. When `self.theory_repo.save(theory)` is called, the repository saves the static inherited `lineage_id` value, creating a mismatch with `theory_lineage.json`.

---

### 6. Defect 1 — Minimum Repair Plan

- **Minimum Code Change**:
  In `market/replay/replay_engine.py`, assign `theory.lineage_id = lineage_id_val` immediately after calling `evolve_theory()`.
- **Affected Files**: [replay_engine.py](market/replay/replay_engine.py).
- **Affected Functions**: `execute()`.
- **Schema / Migration Required**: NO.
- **Backfill Required**: NO.
- **Cognitive Risk**: `NO_CANDIDATE_F_EFFECT` (re-aligns telemetry only).

---

### 7. Defect 2 — Nested TheoryStructured Identity Root Cause

* **Symptom**: Mutated theories receive a new outer theory ID but duplicate the inner structured ID (`5cb3bc75-f0b9-4883-928e-0736d01c715e`) and timestamp in PostgreSQL.
* **Root Cause**: In `REVISE` and `REINFORCE` novelty paths, `replay_engine.py` mutates the prior theory using `theory = copy.deepcopy(prior_theory)`. It updates the outer ID (`theory.id = str(uuid4())`) but fails to regenerate the nested sub-object's ID (`theory.summary_structured.id`) and timestamp.
* **Identity Semantics Matrix**:

| Operation | Field | Semantic Action | Justification |
| :--- | :--- | :--- | :--- |
| **GENERATE** | `summary_structured.id` | `REGENERATE` | Mints a new unique ID for a fresh proposition. |
| **GENERATE** | `summary_structured.created_at` | `REGENERATE` | Mints the compile-time timestamp. |
| **REVISE** | `summary_structured.id` | `REGENERATE` | Revised theories mutate the core prediction parameters, creating a new distinct proposition. |
| **REVISE** | `summary_structured.created_at` | `REGENERATE` | Current compile-time timestamp of the revision step. |
| **REINFORCE** | `summary_structured.id` | `PRESERVE` | Identifies the same physical proposition being reinforced across days. |
| **REINFORCE** | `summary_structured.created_at` | `PRESERVE` | Preserves the original compilation timestamp of the reinforced belief. |

---

### 8. Defect 2 — Minimum Repair Plan

- **Minimum Code Change**:
  Update `replay_engine.py` inside the `REVISE` branch to reset the nested model identifiers:
  ```python
  theory.summary_structured.id = str(uuid4())
  theory.summary_structured.created_at = datetime.now(timezone.utc)
  ```
  Leave `REINFORCE` to preserve the original ID/timestamp to represent belief reinforcement.
- **Affected Files**: [replay_engine.py](market/replay/replay_engine.py).
- **Affected Functions**: `execute()`.
- **Schema / Migration Required**: NO.
- **Backfill Required**: NO.

---

### 9. Defect 3 — Ontology Taxonomy Root Cause

* **Symptom**: The 10-day backtest report lists `0%` valid theory and mechanism components, flagging `SECTOR_ZSCORE` as an unknown value.
* **Root Cause**: The LLM is instructed in the prompt that `sector_zscore` is an available observable variable. The LLM generates components with the concept tag `SECTOR_ZSCORE`. However, `SECTOR_ZSCORE` is omitted from `OntologyRegistry.CORE_CONCEPTS` in `ontology.py`. When `theory_generation_flow.py` runs validation, it rejects the tag as an unknown ontology term, triggering retries and logging compliance failures.
* **Authoritative Source**: `OntologyRegistry` (`cognition/schemas/knowledge/ontology.py`).
* **Cognitive Impact Check**: Does adding `SECTOR_ZSCORE` to `CORE_CONCEPTS` change which generated theories can survive validation?
  - **YES**. When `is_valid` evaluates to `False` due to `SECTOR_ZSCORE` omission, the flow triggers retries and LLM regenerations. Correcting this allows the original generated theory to survive without trigger-retrying.
  - **Classification**: `POTENTIAL_COGNITIVE_EFFECT`. Requires regression testing.

---

### 10. Defect 3 — Minimum Repair Plan

- **Minimum Code Change**:
  Update `OntologyRegistry` by adding `"SECTOR_ZSCORE"` to `CORE_CONCEPTS`.
- **Affected Files**: [ontology.py](cognition/schemas/knowledge/ontology.py).
- **Schema / Migration Required**: NO.

---

### 11. Cross-Defect Interaction Analysis

- **`LINEAGE PROPAGATION` × `NESTED ID REGENERATION`**: Shared code path inside `replay_engine.py`. Resolving both ensures that the outer theory correctly carries the parent's lineage ID, while its nested structured proposition receives a new unique identifier on mutation.
- **`NESTED ID REGENERATION` × `EVIDENCE FOREIGN KEYS`**: Resolving nested ID collisions ensures that validations and prediction probes in SQL can query nested structured theory fields cleanly without key collisions.
- **Implementation Sequence**:
  1. **Step 1: Baseline Preservation Test Run**: Run the 10-day diagnostic replay on unmodified code to log reference decisions.
  2. **Step 2: Lineage ID Propagation Hotfix (Defect 1) + Preservation Test**: Resolves relational lineage without altering decisions.
  3. **Step 3: Nested ID Regeneration Hotfix (Defect 2) + Preservation Test**: Resolves relational uniqueness of the inner structured object without altering decisions.
  4. **Step 4: Cognitive-Behavior Preservation Confirmation**: Confirm byte-for-byte decision identity for Defects 1+2 in isolation.
  5. **Step 5: Ontology Registry Hotfix (Defect 3)**: Fixed last, since it is a known non-neutral change (`POTENTIAL_COGNITIVE_EFFECT`).
  6. **Step 6: Pre/Post Distribution Comparison**: Run characterization analysis for Defect 3.
  *Rationale for Reordering*: Fixing the non-neutral Ontology registry mismatch (Defect 3) first would confound cognitive-behavior preservation checks for Defects 1 and 2. Keeping Defect 3 last isolates its behavioral impact.

---

### 12. Candidate F Preservation Analysis

| Defect / Area | Contradiction Score Impact | TheoryRecord Status Impact | Retirement Behavior Impact | Active Theories Matching | Decision Routing (`REVISE` vs `GENERATE`) |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Defect 1: Relational Lineage** | `NO_CANDIDATE_F_EFFECT` | `NO_CANDIDATE_F_EFFECT` | `NO_CANDIDATE_F_EFFECT` | `TRACEABILITY_ONLY_EFFECT` | `NO_CANDIDATE_F_EFFECT` |
| **Defect 2: Nested ID Collision** | `NO_CANDIDATE_F_EFFECT` | `NO_CANDIDATE_F_EFFECT` | `NO_CANDIDATE_F_EFFECT` (1) | `TRACEABILITY_ONLY_EFFECT` | `NO_CANDIDATE_F_EFFECT` |
| **Defect 3: Ontology Mismatch** | `POTENTIAL_COGNITIVE_EFFECT` | `POTENTIAL_COGNITIVE_EFFECT` | `POTENTIAL_COGNITIVE_EFFECT` (2) | `POTENTIAL_COGNITIVE_EFFECT` | `POTENTIAL_COGNITIVE_EFFECT` |

*(1) Note on Defect 2: A codebase audit confirmed that theory retirement logic in `TheoryLineageEngine.retire_stale_theories` reads `rec.last_seen_step` (an integer step index) to compute `stale_age` and does not query the inner `summary_structured.created_at` timestamp. Resetting this timestamp on `REVISE` does not affect retirement behavior.*

*(2) Note on Defect 3: Adding `SECTOR_ZSCORE` directly impacts the survival of theories on their first attempt, avoiding retries/regenerations. This is expected to alter the composition of active theories, which in turn influences contradiction scores, retirement events, and future decision routing.*

---

### 13. Identity Invariants

The following invariants must hold post-remediation:
- **I1 (Theory ID Uniqueness)**: Every distinct theory node has a unique `theory.id`. (`REQUIRED`)
- **I2 (Structured ID Uniqueness)**: Every distinct nested `TheoryStructured` has a unique `id` and `created_at` timestamp. (`REQUIRED`)
- **I3 (Parent-Child Link)**: Parent-child edges survive persistence. (`REQUIRED`)
- **I4 (Lineage Sharing)**: All members of one lineage family share the correct `lineage_id`. (`REQUIRED`)
- **I5 (Lineage Separation)**: Different lineages do not share the same `lineage_id`. (`REQUIRED`)
- **I6 (Evidence FK)**: Validations correctly reference `theories.id`. (`REQUIRED`)
- **I8 (Reconciliation)**: JSON and SQL lineage states match. (`REQUIRED`)

---

### 14. Taxonomy Invariants

The following invariants must hold post-remediation:
- **T1 (Concept Tag compliance)**: Generated tags like `SECTOR_ZSCORE` are accepted by validation. (`REQUIRED`)
- **T2 (Persisted match)**: Persisted tags match core registers. (`REQUIRED`)
- **T3 (Single Authoritative Source)**: `OntologyRegistry` is the single source of truth. (`REQUIRED`)

---

### 15. Test Strategy

#### A. DEFECT-SPECIFIC UNIT TESTS

* **Test Name**: `test_lineage_propagation`
  - *Setup*: Initialize mock database schemas and active lineage records.
  - *Input*: Call `replay_engine.execute` on a mock 2-step setup.
  - *Expected Output*: Mapped theory has the correct `lineage_id` matching `theory_lineage.json`.
  - *Invariant*: `I4` (Lineage sharing).
  - *Failure Meaning*: Lineage propagation is broken.
* **Test Name**: `test_nested_id_regeneration`
  - *Setup*: Initialize a theory node with nested structured data.
  - *Input*: Trigger `REVISE` copying path.
  - *Expected Output*: Child `summary_structured.id` differs from parent.
  - *Invariant*: `I2` (Structured ID uniqueness).
  - *Failure Meaning*: Deepcopy retains duplicate structured IDs.
* **Test Name**: `test_ontology_compliance`
  - *Setup*: Retrieve the core concept validator registry.
  - *Input*: Validate tag `"SECTOR_ZSCORE"`.
  - *Expected Output*: Returns `is_valid = True`.
  - *Invariant*: `T1` (Concept tag compliance).
  - *Failure Meaning*: `SECTOR_ZSCORE` fails validation checks.

#### B. CROSS-DEFECT INTEGRATION TESTS

* **Test Name**: `test_parent_child_persistence`
  - *Setup*: Clear SQL databases.
  - *Input*: Run mutation and save parent and child.
  - *Expected Output*: Both rows stored in `theories` with matching parent lineage relations.
  - *Invariant*: `I3`.
  - *Failure*: Relation tables missing parent links.
* **Test Name**: `test_distinct_lineage_separation`
  - *Setup*: Create two independent theory families.
  - *Input*: Execute mutation paths on both.
  - *Expected Output*: Evolving lineages carry different `lineage_id` values.
  - *Invariant*: `I5`.
  - *Failure*: Multiple lineages share the same ID.
* **Test Name**: `test_evidence_foreign_key_integrity`
  - *Setup*: Generate a theory and a validation record.
  - *Input*: Call save repository logic.
  - *Expected Output*: Validation `theory_id` matches theory table ID.
  - *Invariant*: `I6`.
  - *Failure*: Foreign keys do not match.
* **Test Name**: `test_prediction_probe_identity`
  - *Setup*: Ingest observation and prediction probe.
  - *Input*: Run save.
  - *Expected Output*: Probe references correct parent theory ID.
  - *Invariant*: `I7`.
  - *Failure*: Probe is unlinked.
* **Test Name**: `test_json_sql_reconciliation`
  - *Setup*: Read JSON snapshot and SQL databases.
  - *Input*: Query IDs for step $t$.
  - *Expected Output*: Both sources contain identical lineage ID mapping tables.
  - *Invariant*: `I8`.
  - *Failure*: State mismatch between database and JSON.

#### C. COGNITIVE-BEHAVIOR PRESERVATION TESTS

* **Test Name**: `test_pre_post_cognitive_behavior_preservation`
  - *Setup*: Run baseline and hotfixed code (Defects 1+2 only) under identical LLM seed.
  - *Input*: Run full 10-day backtest replay window.
  - *Expected Output*: Outer decision sequence (`REVISE`/`GENERATE`/`REINFORCE`) remains byte-for-byte identical across all 10 steps.
  - *Invariant*: Governing Remediation Invariant.
  - *Failure*: Any cognitive decision diverges at any step in the 10-day window.
* **Test Name**: `test_ontology_cognitive_effect_distribution`
  - *Setup*: Run theory generation prompts/seeds.
  - *Input*: Compare execution BEFORE the ontology fix (`SECTOR_ZSCORE` absent from `CORE_CONCEPTS`) and AFTER the fix (`SECTOR_ZSCORE` present).
  - *Expected Output / Report*:
    1. Count of theories that triggered validation failure/retry (due to `SECTOR_ZSCORE` error) pre-fix but survive on first attempt post-fix.
    2. Comparison of surviving theory content between the pre-fix retried/regenerated version and the post-fix first-attempt version for the same originating prompt to check for systematic bias.
  - *Classification*: Disclosed cognitive-behavioral delta (non-neutral effect).
  - *Failure*: Validation failures on `SECTOR_ZSCORE` post-fix.

---

### 16. Before / After Acceptance Criteria

* **Defect 1**:
  - *Before*: SQL `theories.lineage_id` is a static, random UUID for all rows.
  - *After*: SQL `theories.lineage_id` matches the lineage IDs in `theory_lineage.json`.
* **Defect 2**:
  - *Before*: Mutated theories duplicate nested structured IDs.
  - *After*: Every mutated theory gets a unique `summary_structured.id` in PostgreSQL.
* **Defect 3**:
  - *Before*: `SECTOR_ZSCORE` is flagged as an unknown taxonomy term (0% compliance).
  - *After*: Replay compliance metrics report 100% compliance for components using `SECTOR_ZSCORE`.

---

### 17. Small Diagnostic Replay Plan

We justify using the **10-day diagnostic replay window** as the minimum required period to ensure that all cognitive lifecycle events (theory generation, mutation, evidence, contradiction, retirement, active theories retrieval, and ontology check) are naturally reached and exercised in the actual financial backtest environment.
- **Command**: `POSTGRES_HOST=127.0.0.1 poetry run python -m market.replay.run --days 10 --restart --reset`
- **Environment**: Local PostgreSQL active.
- **Pass Criteria**: SQL table has 10 unique theories, no structured ID collisions, and 100% ontology compliance.

---

### 18. Historical Data and Migration Decision

To protect scientific history and prevent silent alterations to historical evidence:
- **Decision**: Existing database tables and snapshots will be copied to `data/archive/ARCHIVED_AS_UNTRUSTED_PRE_REMEDIATION_DIAGNOSTIC` folder and preserved.
- **Namespace**: Post-remediation runs will write to the `_trusted_post_remediation` database schema and log namespace.
- No backfill of historical runs is authorized.

---

### 19. Exact Implementation Sequence

1. **Step 1: Baseline Preservation Test Run**
   - Target Codebase: Baseline (before any fixes).
   - Action: Run full 10-day replay under seed 42 to log reference decision sequence (`REVISE`/`GENERATE`/`REINFORCE`).
2. **Step 2: Lineage Propagation Hotfix (Defect 1)**
   - Target Codebase: Baseline + Defect 1 fix.
   - Files: `market/replay/replay_engine.py`.
   - Test: `test_lineage_propagation`. Verify lineage matches snapshot JSON, and decisions match baseline.
3. **Step 3: Nested ID Reset Hotfix (Defect 2)**
   - Target Codebase: Baseline + Defects 1 & 2 fixes.
   - Files: `market/replay/replay_engine.py`.
   - Test: `test_nested_id_regeneration`. Verify nested IDs/timestamps are regenerated on `REVISE`.
4. **Step 4: Confirm Preservation of Defects 1+2 in Isolation**
   - Action: Run 10-day replay on Defects 1+2 code. Confirm byte-for-byte decision sequence matches baseline.
5. **Step 5: Ontology Registry Hotfix (Defect 3)**
   - Target Codebase: Baseline + Defects 1, 2, & 3 fixes.
   - Files: `cognition/schemas/knowledge/ontology.py`.
   - Test: `test_ontology_compliance`.
6. **Step 6: Pre/Post Distribution Comparison for Defect 3**
   - Action: Run the new distribution comparison test to characterize and report the non-neutral cognitive effect.

*Rollback condition*: If any test fails, run `git reset --hard <commit>` to revert the step.

---

### 20. Claude Adversarial Review Handoff and Final Recommendation

The handoff package asks Claude to challenge:
1. Do the proposed changes to `replay_engine.py` alter any cognitive novelty gate decisions?
2. Does adding `SECTOR_ZSCORE` to `CORE_CONCEPTS` change prompt generation or LLM outputs?
3. Can the 10-day diagnostic replay detect hidden coupling between nested ID regeneration and the validation engine?

**Final Recommendation**:
`REMEDIATION_PLAN_READY_FOR_CLAUDE_ADVERSARIAL_REVIEW`
